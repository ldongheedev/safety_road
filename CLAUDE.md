# SafetyRoad 프로젝트 규칙

## Git 푸시 규칙

깃허브 저장소에 푸시하기 전에 파일의 내용을 확인하고, API 키값 등 유출되면 안 될만한 파일들이 `.gitignore` 안에 잘 들어가 있는지를 매 푸시할 때마다 확인.

## 추가 오류 발견 시 규칙

작업 중 요청하지 않은 다른 오류나 개선사항을 발견하더라도 임의로 수정하지 않는다. 발견한 내용을 브리핑하고 수정 여부를 반드시 먼저 확인받은 후 진행한다.

## 재시작 알림 규칙

백엔드와 프론트엔드가 실행 중인 상태라고 가정한다. 오류 수정 또는 기능 개발 완료 후, 변경 내용을 적용하기 위해 재시작이 필요한 경우 작업 완료 직후 아래 기준에 따라 반드시 명시적으로 알린다.

- 백엔드(Spring Boot) 파일 변경 시 → "백엔드 재시작이 필요합니다"
- 프론트엔드(React) 파일 변경 시 → "프론트엔드 재시작이 필요합니다"
- 양쪽 모두 변경 시 → 둘 다 알린다
